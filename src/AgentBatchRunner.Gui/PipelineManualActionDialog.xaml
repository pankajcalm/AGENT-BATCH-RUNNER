<Window x:Class="AgentBatchRunner.Gui.PipelineManualActionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manual Pipeline Status"
        Width="620"
        Height="510"
        MinWidth="520"
        MinHeight="430"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock x:Name="HeadingText" Grid.Row="0" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
    <TextBlock x:Name="FileText" Grid.Row="1" Margin="0,5,0,14" TextWrapping="Wrap" />

    <TextBlock Grid.Row="2" Margin="0,0,0,4" FontWeight="SemiBold" Text="Reason (required)" />
    <TextBox x:Name="ReasonTextBox" Grid.Row="3" MinHeight="90" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />

    <Grid Grid.Row="4" Margin="0,12,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center" FontWeight="SemiBold" Text="Evidence/report" />
      <TextBox x:Name="EvidenceTextBox" Grid.Column="1" Margin="0,0,8,0" />
      <Button Grid.Column="2" Width="82" Click="BrowseEvidence_Click" Content="Browse" />
    </Grid>

    <StackPanel Grid.Row="5" Margin="0,12,0,0">
      <TextBlock Margin="0,0,0,4" FontWeight="SemiBold" Text="Notes (optional)" />
      <TextBox x:Name="NotesTextBox" MinHeight="58" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
      <CheckBox x:Name="SatisfyDependenciesCheckBox" Margin="0,12,0,0" Content="Treat as satisfying dependencies" />
      <CheckBox x:Name="ApproveGateCheckBox" Margin="0,8,0,0" Content="Treat declared gate as approved (requires evidence)" />
      <TextBlock x:Name="ValidationText" Margin="0,10,0,0" Foreground="#B42318" TextWrapping="Wrap" />
    </StackPanel>

    <StackPanel Grid.Row="6" Margin="0,16,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Width="88" Margin="0,0,8,0" IsCancel="True" Content="Cancel" />
      <Button x:Name="ConfirmButton" Width="150" IsDefault="True" Click="Confirm_Click" Content="Confirm" />
    </StackPanel>
  </Grid>
</Window>
