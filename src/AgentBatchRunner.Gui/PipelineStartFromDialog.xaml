<Window x:Class="AgentBatchRunner.Gui.PipelineStartFromDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Start Pipeline From Selected"
        Width="650"
        Height="460"
        MinWidth="540"
        MinHeight="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" FontSize="18" FontWeight="SemiBold" Text="Start pipeline from selected file" />
    <TextBlock x:Name="TargetText" Grid.Row="1" Margin="0,5,0,14" TextWrapping="Wrap" />
    <ListBox x:Name="ImpactList" Grid.Row="2" FontFamily="Consolas" FontSize="12" />
    <TextBlock x:Name="ReasonText" Grid.Row="3" Margin="0,12,0,0" TextWrapping="Wrap" />
    <StackPanel Grid.Row="4" Margin="0,16,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button x:Name="SelectPrerequisiteButton" Width="142" Margin="0,0,8,0" Click="SelectPrerequisite_Click" Content="Select Prerequisite" />
      <Button Width="88" Margin="0,0,8,0" IsCancel="True" Content="Cancel" />
      <Button x:Name="StartButton" Width="118" IsDefault="True" Click="Start_Click" Content="Start Pipeline" />
    </StackPanel>
  </Grid>
</Window>
